{
  "name": "tree-view-auto-fold",
  "main": "./lib/main",
  "version": "0.0.0",
  "description": "This package is a plugin on top of the tree-view, to fold all unmodified / closed files until explicitly browsed for.",
  "keywords": [
    "tree-view",
    "fold",
    "git-status",
    "open"
  ],
  "repository": "https://github.com/NutterV/atom-tree-view-auto-fold",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  }
}
